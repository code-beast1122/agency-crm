services:
  - type: web
    name: agency-crm
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.enableCORS false --server.enableWebsocketCompression false
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SERVICE_ROLE_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: CLOCKIFY_API_KEY
        sync: false
      - key: CLOCKIFY_WORKSPACE_ID
        sync: false
      - key: GOOGLE_CALENDAR_ID
        sync: false
