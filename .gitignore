.env
venv/
__pycache__/
*.pyc
# Keep Streamlit's secrets out, but let the theme config ship -- it holds no
# secrets, and without it the deployed app renders un-themed.
.streamlit/*
!.streamlit/config.toml
.streamlit/secrets.toml
credentials.json